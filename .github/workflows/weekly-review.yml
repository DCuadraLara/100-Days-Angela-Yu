name: Weekly Review & PDF

on:
  schedule:
    - cron: '0 7 * * 1'
  push:
    paths:
      - 'exercises/**'
      - 'tools/**'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install reportlab pyyaml

      - name: Run reviewer
        run: python tools/reviewer.py

      - name: Commit reports and PDF
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/ pdf_report_weekly/
          git commit -m "chore: weekly review (reports + PDF)" || echo "nothing to commit"
          git pull --rebase || true
          git push || true

      - name: Upload weekly PDF
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: weekly-pdf
          path: pdf_report_weekly/*.pdf

      # Email usando connection_url (smtps en 465)
      - name: Send email notification
        if: always()
        uses: dawidd6/action-send-mail@v3
        with:
          connection_url: smtps://${{ secrets.SMTP_USERNAME_URL }}:${{ secrets.SMTP_PASSWORD }}@smtp.gmail.com:465
          from: ${{ secrets.MAIL_FROM }}
          to: ${{ secrets.MAIL_TO }}
          subject: "Weekly review ✅ 100 Days of Code"
          html_body: |
            <h3>Revisión semanal completada</h3>
            <p>Se han generado reports en <code>/reports</code> y un PDF en <code>/pdf_report_weekly</code>.</p>
            <p>También puedes descargar el PDF desde los artifacts del run.</p>
            <p>¡A seguir sumando! 💪</p>
